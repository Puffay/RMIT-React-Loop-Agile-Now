const Navbar = () => {
    return ( 
        <nav className="navbar">
            <h1>Loop Agile Now</h1>
            <div className="links">
                <a href="/">Home</a>
                <a href="/signup">Sign Up</a>
            </div>
        </nav>
    );
}

export default Navbar;